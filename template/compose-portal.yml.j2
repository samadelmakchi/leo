services:
  {{ customer_containers }}-portal:
    image: node:22
    working_dir: /app
    command: bash -c "npm install && npm run dev"
    volumes:
      - ../:/app
    restart: unless-stopped
    networks:
      - traefik_reverse_proxy
    environment:
      INSTANCE_NAME: {{ customer_containers }}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.{{ customer_containers }}-portal.rule=Host(`{{ customer_subdomain_portal }}.{{ customer_domain }}`)"
      - "traefik.http.routers.{{ customer_containers }}-portal.entrypoints=websecure"
      - "traefik.http.routers.{{ customer_containers }}-portal.tls.certresolver=myresolver"
      - "traefik.http.services.{{ customer_containers }}-portal.loadbalancer.server.port=5173"

networks:
  traefik_reverse_proxy:
    external: true


#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# فراخوانی با نود
# 5173
    # image: node:22
    # working_dir: /app
    # command: bash -c "npm install && npm run dev"

# فراخوانی با انجینکس
# 80
    # build:
    #   context: {{ project_path }}/{{ inventory_hostname }}/portal
    #   dockerfile: docker/Dockerfile

#~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~